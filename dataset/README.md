# dataset/

Build output of `pipeline/`, committed on purpose: the app must run with no network
access, and a checked-in dataset is what makes a build reproducible from a git tag.

Nothing is generated yet. Phase 0.6 fills this directory; do not hand-edit it.
